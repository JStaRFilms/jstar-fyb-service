# 🎯 Task: Progress Tracking System

**Objective:** Implement dynamic progress tracking for projects based on actual chapter completion and word counts.
**Priority:** Medium
**Scope:** Project model, stat calculation, UI updates

---

## 📋 Requirements

**[REQ-001]** Calculate `progressPercentage` dynamically
- Based on: chapters complete / total chapters
- Or: words written / target word count
- Update on chapter save

**[REQ-002]** Calculate total word count
- Sum of all chapter `wordCount` fields
- Display in workspace stats (replace hardcoded "2.4k")

**[REQ-003]** Update stats in real-time
- `MobileTimelineView.tsx` should receive dynamic data
- `TimelineSidebar.tsx` (desktop) should show similar stats

---

## 🔍 Current State

### Schema Fields (Already Exist)
```prisma
model Project {
  progressPercentage Int @default(0)  // 0-100
  contentProgress    Json?
  timeTracking       Json?
  milestones         Json?
}
```

### Hardcoded Values to Replace
- `MobileTimelineView.tsx:22` → "2.4k" words
- `MobileTimelineView.tsx:31` → "15%" complete

---

## 🏗️ Implementation

1. Create `calculateProjectProgress(projectId)` service function
2. Call on chapter save/update
3. Pass real values to `MobileTimelineView` and `TimelineSidebar`

---

*Generated by /spawn_task workflow*
